import XCTest
@testable import PlayerFeature

final class PlayerFeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify the behavior of your code.
        XCTAssertTrue(true, "PlayerFeature package loads successfully")
    }
}