# 2026-01-27 — Split RSS feed parser tests to satisfy SwiftLint

**Intent**
- Remove `type_body_length` violation in `RSSFeedParserTests` without altering behavior or coverage.

**Plan**
1) Break the oversized test class into focused suites (episodes, podcast metadata/fixtures, edge cases).
2) Extract the shared `WarningRecorder` helper to a tiny utility file.

**Outcome**
- New test files: `RSSFeedParserEpisodeTests`, `RSSFeedParserPodcastTests`, `RSSFeedParserEdgeCaseTests`, plus `WarningRecorder`.
- No test logic changes; grouping only. Each class now well under the 500-line limit. Lint should pass on next run.
