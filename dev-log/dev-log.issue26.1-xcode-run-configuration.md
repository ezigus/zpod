

## Update Log
- 2025-09-27 20:38 ET: Restored shared `zpod` scheme (app target) and refreshed `run-xcode-tests.sh` default plus simulator fallbacks to avoid confusion with the SwiftPM auto-generated scheme.
- 2026-02-21 19:33 ET: Fixed `run-xcode-tests.sh` summary/reporting gaps for UI-suite boot failures.
  - Added UI summary fallback so failed `zpodUITests/*` suites still appear in `Test Results` even when no suite-level xcresult timing rows are available.
  - Added UI timing fallback to derive per-suite timing from phase entries when xcresult parsing has no test nodes.
  - Normalized group totals in test aggregation to prevent inconsistent output where `total < passed + failed + skipped`.
  - Fixed boot failure status propagation in two call sites by capturing the real `boot_simulator_destination` exit code (instead of the `if ! ...` inverted status), so simulator boot timeouts now fail the run with non-zero exit.
  - Added UI orchestration recovery: after first suite failure (without fresh-sim mode), remaining suites automatically switch to fresh simulators and reset CoreSimulator service once.
- 2026-02-22 06:41 ET: Fixed duplicate end-of-run summaries caused by trap inheritance in lint/test pipelines.
  - Root cause: child shells spawned by pipelines inherited `EXIT`/`ERR` traps and could call `finalize_and_exit`, emitting an extra summary block before the parent process finalized.
  - Added root-shell PID guards to `handle_exit`, `handle_unexpected_error`, and `handle_interrupt` so only the original `run-xcode-tests` process can finalize/report.
  - Validation: `./scripts/run-xcode-tests.sh -l` now emits a single summary block (`TestResults/TestResults_20260222_064101_lint_swift.log` contains one `Overall Status` section).
- 2026-02-22 07:31 ET: Investigating remaining summary consistency gaps after targeted failures.
  - Reproduced current behavior where failed targeted UI runs can end summary output after `Test Results` (missing `Overall Status` block).
  - Reproduced `-t Packages` timing gaps where package detail rows display `00:00:00` durations.
  - Planned fix direction:
    - make summary rendering resilient under `set -e` so final blocks always print,
    - ensure package suite execution records per-package phase timing in targeted mode,
    - normalize group classifiers to avoid non-zero return codes from helper matchers.
- 2026-02-22 07:35 ET: Implemented summary stability and package timing fixes in `scripts/lib/harness.sh`.
  - Hardened summary classifier helpers (`phase_group_for`, `test_group_for`) to always return success, preventing `set -e` aborts during command substitutions while rendering summary sections.
  - Added targeted run mapping for `Test Packages` so package tests are grouped under `Package Tests` in timing/results blocks.
  - Added `PACKAGE_TEST_TIMING_ENTRIES` plus `record_package_test_timing` and wired `test_package_target` to record elapsed time for success, warn, and error outcomes.
  - Updated `package_test_timing_breakdown` to consume recorded per-package timings (with legacy phase fallback), so targeted package runs no longer show zeroed durations.
  - Updated `finalize_and_exit` to temporarily disable `errexit` while printing the final summary to avoid truncated output when helper blocks return non-zero.
- 2026-02-22 07:40 ET: Fixed phase failure handling to prevent premature finalize on targeted UI failures.
  - Root cause: `execute_phase` used a plain command invocation under an active `ERR` trap; non-zero phase commands triggered `handle_unexpected_error` before phase timing/status bookkeeping and before later targets could run.
  - Replaced `set +e` invocation flow with `if "${command[@]}"; then ... else ... fi` status capture so non-zero phase results are handled as expected inside phase orchestration without invoking the global trap.
  - Expected impact: failing targeted suites no longer skip phase timing aggregation and no longer prevent subsequent requested targets from running.
- 2026-02-22 07:44 ET: Updated explicit `-t` execution loop to run all requested targets even when one fails.
  - Root cause: in the `REQUESTED_TESTS` loop, a failing `execute_phase` returned non-zero and global `set -e` exited the script before remaining targets.
  - Wrapped each explicit test phase invocation in `if ! execute_phase ...; then :; fi` so failures still contribute to summary/exit status while subsequent targets continue.
  - Expected impact: `./scripts/run-xcode-tests.sh -t targetA,targetB,...` now executes every requested target and reports aggregated results.
- 2026-02-22 07:52 ET: Verified combined package + UI targeted run after summary fixes.
  - Validation command: `./scripts/run-xcode-tests.sh -t Packages,zpodUITests/PlayerAccessibilityTests,zpodUITests/SwipePresetSelectionTests`
  - Observed behavior: both requested UI targets executed in a single run even though `PlayerAccessibilityTests` failed.
  - Summary behavior: single summary instance, complete `Build Timing`/`Test Timing`/`Test Results`/`Overall Status` blocks, package timing rows populated with non-zero durations, and both UI suites listed in timing + results breakdown.
- 2026-02-22 08:02 ET: Planned UI concurrency controls for issue #187 follow-up.
  - Goal: prevent overlapping UI test runs across concurrent `run-xcode-tests.sh` invocations while keeping package/integration/lint phases unconstrained.
  - Chosen approach:
    - machine-local UI lock directory with PID-backed metadata + stale lock recovery,
    - fail-fast conflict behavior with explicit owner diagnostics in console + summary output,
    - opt-in UI sharding mode (`ZPOD_UI_TEST_PARALLELISM`) to run multiple UI suites concurrently inside one invocation.
- 2026-02-22 08:11 ET: Implemented UI lock + optional sharding in `scripts/lib/harness.sh`.
  - Added lock lifecycle helpers (`acquire_ui_test_lock`, `release_ui_test_lock`, metadata read/write, stale cleanup) using an atomic lock-directory strategy.
  - Added conflict reporting with owner PID/run-id/host/command and explicit stale-lock recovery guidance in output.
  - Wired UI lock acquisition into both paths:
    - explicit `-t` UI targets via `run_test_target`,
    - full-regression UI orchestration via `run_ui_test_suites`.
  - Added fail-fast handling for lock/shard setup failures while preserving existing behavior for ordinary UI test failures.
  - Added run identifier propagation (`run_id`) to start/end logs and `Overall Status` summary block for easier multi-run attribution.
  - Added opt-in sharded UI execution:
    - `ZPOD_UI_TEST_PARALLELISM` / `ZPOD_UI_TEST_PARALLEL_MAX` worker control,
    - per-worker simulator provisioning + per-worker DerivedData roots,
    - worker report merge back into parent summary/timing structures.
  - Updated `--help` environment documentation for all new UI concurrency knobs.
- 2026-02-22 08:11 ET: Validated lightweight lock behavior and summary output.
  - Validation commands:
    - `./scripts/run-xcode-tests.sh --self-check`
    - lock-conflict simulation using `ZPOD_UI_TEST_LOCK_DIR=/tmp/... ./scripts/run-xcode-tests.sh -t zpodUITests/PlayerAccessibilityTests`
  - Observed behavior:
    - lock conflict exits immediately with code `73`,
    - output includes owner diagnostics and stale-lock guidance,
    - final summary remains single-instance and includes `Run ID` + complete `Overall Status` block.
- 2026-02-22 08:13 ET: Refined conflict summary counts for operator clarity.
  - Updated lock-conflict summary entry to contribute explicit failed totals (`total=1, failed=1`) instead of zero-count rows.
  - Revalidated lock-conflict simulation and confirmed `Test Results` now reports the lock conflict as a failed UI suite line item.
- 2026-02-22 08:17 ET: Added explicit UI lock clearing commands for operator recovery.
  - Added `--clear-ui-lock` option to `scripts/run-xcode-tests.sh` (implemented in `scripts/lib/harness.sh`) to remove the current UI lock directory and exit.
  - `--clear-ui-lock` is intentionally standalone; combining it with test/build/syntax/lint/self-check flags now returns an error.
  - Added `clear_ui_test_lock()` helper to report lock owner metadata (when present), distinguish active vs stale lock owners, and clear lock state safely.
  - Added convenience wrapper script `scripts/clear-ui-test-lock.sh` that invokes `run-xcode-tests.sh --clear-ui-lock`.
- 2026-02-22 08:18 ET: Validated UI lock-clear command paths.
  - Validation commands:
    - `./scripts/run-xcode-tests.sh --clear-ui-lock` (no lock present)
    - simulated lock + `ZPOD_UI_TEST_LOCK_DIR=/tmp/... ./scripts/run-xcode-tests.sh --clear-ui-lock`
    - simulated lock + `ZPOD_UI_TEST_LOCK_DIR=/tmp/... ./scripts/clear-ui-test-lock.sh`
    - `./scripts/run-xcode-tests.sh --clear-ui-lock -s` (standalone enforcement)
  - Observed behavior:
    - no-lock case exits cleanly with informative message,
    - lock directory is removed in both direct flag and wrapper script flows,
    - combined-flag usage is rejected with explicit error.
- 2026-02-22 08:20 ET: Ensured shard workers reuse the prebuilt host app.
  - Problem: each shard previously looked inside its own derived-data folder for `zpod.app`, but build-for-testing builds only once into a single derived root, so the per-worker paths didn’t contain the host app and every suite failed.
  - Fix: compute a shared derived-data root before `build_for_testing_phase` when `ZPOD_UI_TEST_PARALLELISM>1`, export it via `ZPOD_DERIVED_DATA_PATH`, and let every shard use that same root (`UI_PARALLEL_SHARED_DERIVED_ROOT`) so `ensure_host_app_product()` succeeds.
  - Impact: shards now inherit a real `zpod.app`, making the UI suites run instead of reporting repeated “System Failure” entries.
- 2026-02-22 12:40 ET: Added host-app preflight for parallel shards.
  - Added `UI_PARALLEL_SHARED_DERIVED_ROOT` handling for all invocations and ensure it is created before the build phase runs.
  - Introduced `ensure_shared_host_app_product()` to verify `zpod.app` exists in that shared root and fail early if it does not, avoiding the later "bundle identifier" failure and giving a descriptive error.
  - The new guard also emits an error summary entry if the shared host app is missing, making debugging easier before a shard even starts.
- 2026-02-22 12:28 ET: Improved UI timing and summary counts for shard runs.
  - Added an explicit `UI Tests` line back into the `Test Timing` block so UI durations show up just before the package timing detail.
  - Clamped computed UI pass counts (both aggregated from timing entries and from summary fallbacks) so the output never displays `-1` when totals are incomplete.
  - Verified `./scripts/run-xcode-tests.sh -s` still passes after these formatting tweaks.
- 2026-02-22 14:14 ET: Fixed negative UI pass counts and shard merge duplication in summary output.
  - Added `normalize_suite_counts()` and applied it across UI suite aggregation/printing paths so rows with `total=0, failed=1` normalize to `total=1, passed=0` (eliminates `✅ -1`).
  - Updated `record_test_suite_timings()` ingestion (xcresult and log fallback) to normalize suite totals before storing `TEST_SUITE_TIMING_ENTRIES`.
  - Added merge dedupe guards (`append_summary_item_if_new`, `append_test_suite_timing_entry_if_new`) so shard worker report replay cannot duplicate rows/counters.
  - Verified with synthetic inputs that UI summary now renders stable counts and no duplicate suite entries.
- 2026-02-22 15:12 ET: Routed explicit `-t zpodUITests` through shard orchestration when parallelism is enabled.
  - Root cause: explicit target handling called `test_app_target zpodUITests`, which runs one monolithic `xcodebuild -only-testing:zpodUITests` invocation and bypasses per-suite sharding.
  - Fix: in `run_test_target`, detect `target == zpodUITests` with `UI_PARALLELISM > 1` and delegate to `run_ui_test_suites`.
  - Expected behavior: `ZPOD_UI_TEST_PARALLELISM=2 ./scripts/run-xcode-tests.sh -t zpodUITests` now executes suite shards instead of a single all-tests run.
- 2026-02-22 15:17 ET: Fixed explicit `-t zpodUITests` sharding prebuild path.
  - Root cause: sharded explicit UI runs required a prebuilt host app in shared derived data, but the `-t zpodUITests` path did not run build-for-testing beforehand.
  - Fixes in `scripts/lib/harness.sh`:
    - `run_ui_test_suites_parallel`: when host app is missing, run `build_for_testing_phase` preflight, record build timing, then re-check host app.
    - `run_test_target`: when routing `zpodUITests` to shard orchestration, force `ZPOD_TEST_WITHOUT_BUILDING=1` for shard suite executions and restore previous value afterward.
  - Expected behavior: `ZPOD_UI_TEST_PARALLELISM=2 ./scripts/run-xcode-tests.sh -t zpodUITests` now auto-prebuilds once (if needed) and then runs sharded UI suites without per-suite rebuilds.
- 2026-02-22 16:16 ET: Corrected UI timing inflation for sharded runs.
  - Root cause: shard report merge was adding per-suite `record_phase_timing` entries, and `Test Timing -> UI Tests` summed those durations, which double-counted parallel work.
  - Fixes:
    - Removed per-suite phase timing writes from `append_ui_worker_report_entries`.
    - Wrapped default-pipeline UI orchestration in a single phase (`execute_phase "UI tests" "test" run_ui_test_suites`) so `UI Tests` timing is a wall-clock phase duration in both default and explicit target flows.
  - Expected behavior: UI suite detail still lists per-suite durations, while `UI Tests` total aligns with overall elapsed wall-clock instead of serial sum.
- 2026-02-22 16:28 ET: Implemented dynamic UI shard scheduling with worker-stop visibility.
  - Replaced static upfront suite partitioning with a dynamic queue (`pending`/`inflight`/`done`) so workers claim suites as they become available.
  - Added queue helpers (`init_ui_dynamic_queue`, `claim_next_ui_suite`, `complete_ui_suite_claim`, `queue_remaining_count`) and switched shard workers to pull work in a loop.
  - Added worker health reporting (`worker_health|...`) and summary rendering (`UI shard worker health`) that explicitly reports unexpected worker stops with exit code, last suite, completed/claimed counts, and remaining queued/inflight suites.
  - Added degraded-run handling when workers stop unexpectedly or when queue items remain unprocessed after workers finish.
- 2026-02-22 18:27 ET: Added cancel-safe teardown for dynamic UI shards.
  - Added global shard runtime registry (worker PIDs/labels/reports/simulators, active/teardown flags, queue roots) so trap handlers can cleanly unwind parallel work.
  - Implemented `teardown_ui_shard_runtime(reason, emit_summary)`:
    - signals shard workers (`TERM`, optional grace via `ZPOD_UI_SHARD_KILL_GRACE_SECONDS`, then `KILL` fallback),
    - reaps workers,
    - merges available worker reports,
    - records stopped-worker health entries when completion markers are missing,
    - cleans up dedicated shard simulators,
    - emits a cancellation summary row when requested.
  - Wired teardown into `INT`, `ERR`, and `EXIT` trap paths and added a finalize guard so UI lock release cannot happen before shard teardown.
  - Updated parallel orchestrator to register workers/simulators in global runtime state and to use teardown during partial startup failure (e.g., simulator provisioning failure after some workers already started).
- 2026-02-22 18:56 ET: Enhanced `--clear-ui-lock` to include shard artifact cleanup and active-owner termination.
  - Added process-tree termination (`collect_descendant_pids`, `terminate_process_tree`) so clear-lock can stop active background jobs tied to lock ownership before removing the lock.
  - Added cleanup of UI shard artifacts in `TMPDIR` (`zpod-ui-derived-*` directories and matching lock artifact files) as part of clear-lock.
  - Updated owner liveness detection to treat zombie (`Z`) processes as not alive, preventing false clear-lock failures immediately after termination.
- 2026-02-22 19:58 ET: Worker health summary now prints for all sharded runs, not only crashes.
  - `print_ui_worker_health_summary` now renders when any worker health entries exist.
  - Output includes per-worker completion stats (completed/claimed suites, last suite, remaining queue) and flags whether that worker encountered suite failures.
  - Unexpected worker stops are still highlighted as error lines.
